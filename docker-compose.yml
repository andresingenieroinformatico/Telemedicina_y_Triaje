version: '3.8'

services:
  web:
    build: .
    container_name: triage_api
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/venv  # Evita que el venv local de Windows interfiera con el contenedor
    environment:
      - FLASK_ENV=development
